<section class="search-results">

    <header class="search-results__header">
        <h2>Results</h2>
    </header>

    <ul id="search-result-list"></ul>

</section>

<style>
.search-results {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
}

.search-results__header {
    padding: var(--space-3) var(--space-4);
    border-bottom: 1px solid var(--color-border);
}

.search-results__header h2 {
    margin: 0;
    font-size: var(--font-size-xl);
}

#search-result-list {
    display: flex;
    flex-direction: column;
}

#search-result-list:empty {
    display: none;
}

#search-result-list :global(li) {
    margin: 0;
    padding: 0;
}

#search-result-list :global(li + li) {
    border-top: 1px solid var(--color-border);
}

#search-result-list :global(a) {
    display: flex;
    align-items: center;
    justify-content: space-between;

    gap: var(--space-4);

    min-height: 60px;
    padding: var(--space-3) var(--space-4);

    color: var(--color-text);

    background: var(--color-surface);

    transition:
        color 150ms ease,
        background-color 150ms ease;
}

#search-result-list :global(a:hover) {
    color: var(--color-primary);
    background: var(--color-background);
}

#search-result-list :global(.search-result__name) {
    min-width: 0;

    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;

    font-weight: var(--font-weight-medium);
}

#search-result-list :global(.search-result__type) {
    flex-shrink: 0;

    color: var(--color-text-muted);

    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);

    text-transform: uppercase;
    letter-spacing: 0.04em;
}

@media (max-width: 600px) {

    #search-result-list :global(a) {
        min-height: 56px;
        padding-inline: var(--space-3);
    }
}
</style>